// Config di debug — per ora committato di proposito: il menu resta attivo
// anche in produzione. Il file è nella precache del service worker: se lo
// togli, rimuovilo anche dalla lista `files` in service-worker.js.
window.DEBUG_MENU_ENABLED = true;
